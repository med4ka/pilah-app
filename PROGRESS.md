# PROGRESS.md — Pilah Rework Tracker

Latest status update: **Phase 2 — store refactor done** (auth/ui/pickup split)

## Phase 0 — Document Foundation
- [x] PRD.md
- [x] DESIGN.md
- [x] SYSTEM.md
- [x] ARCHITECTURE.md
- [x] RULES.md
- [x] PROGRESS.md (this document)

## Phase 1 — Security & Architecture Fix (Backend)
- [x] Remove hardcoded JWT secret fallback, require from env (fail-fast)
- [x] Add ownership check in `UserConfirmPickup` (`pickup.UserID == current_user`)
- [x] Add ownership check in `CompletePickup` (`pickup.CollectorID == current_user`)
- [x] Fix `AcceptPickup` with row lock (`FOR UPDATE`) to prevent race condition of two collectors accepting simultaneously
- [x] Wrap status + karma update in one correct transaction (clear commit/rollback)
- [x] Move all direct queries from `handlers/pickup.go` & `handlers/collector.go` to the repository layer
- [x] Add `RequireRole` middleware
- [x] Migrate auth from `Authorization` header → httpOnly cookie
- [~] Add index `pickups(status, created_at)` — file `backend/migrations/0001_add_pickup_status_index.sql` already created, **not yet run** (waiting for Ghif to run manually in pgAdmin/psql)

## Phase 2 — Frontend Refactor
- [x] Split `usePilahStore` into slices: auth / ui / pickup (`useAuthStore`, `useUIStore`, `usePickupStore` placeholder; old file deleted)
- [x] Remove token from localStorage/persist, replace detection via `GET /users/me`
- [x] Refactor `lib/api.ts` into a fetch wrapper with `credentials: include`
- [x] Audit all components in `app/components/dashboard` — inventory which are functional vs pure placeholder

## Additional task: warga confirmation UI (blocker found during audit)
- [x] Backend: pickup lifecycle becomes `PENDING → ACCEPTED → VERIFYING → COMPLETED` — `CompletePickup` (collector) now sets VERIFYING + saves weight per material & photo evidence (without karma), `ConfirmPickup` (warga) newly moves to COMPLETED + transfers karma and the response carries `karma`
- [x] Frontend: `confirmPickup()` in `lib/api.ts` + `ActivePickupCard` component (polls status every 5 seconds, shows plastic/cardboard/glass weight breakdown + photo evidence, "Konfirmasi Selesai" button ONLY when status VERIFYING, karma feedback after confirm)
- [x] Placeholder cleanup (no new features): `DropPointSheet` "Arahkan" button → real Google Maps link (target _blank), `RewardSheet` & `HelpTab` → "Segera Hadir" state, `DashboardBanner` remove dead "Mulai Sekarang" button, `ServiceGrid` remove "Lainnya" entry
- [~] Migration `0002_add_pickup_verification_fields.sql` (weight+photo columns) already created, **not yet run manually** (AutoMigrate already added columns during dev)

## Phase 3 — UI/UX Redesign (per DESIGN.md)
- [~] Apply new color palette & typography — **base tokens done** (`globals.css` `@theme` + `:root`), components not yet touched
  - [x] Design tokens in `globals.css`: `--color-primary` (emerald-700), status (pending/verifying amber-600, accepted blue-600, completed emerald-600, error red-600), `neutral-50..900` scale, `--radius-base` 12px, `--shadow-soft`; dark-mode block removed (Phase 2 optional), Arial fallback in `body` removed (use Geist via layout)
- [x] Redesign pickup status stepper (Pending → Accepted → Verifying → Completed) — new `PickupStatusStepper` component, installed in `ActivePickupCard` (uses CSS tokens, smooth 2s pulse, lucide "Check" icon)
- [ ] Redesign empty states (remove "sticker-like" elements if any)
- [x] Redesign Mitra flow (radar list) — dense & functional
- [x] Redesign Warga flow (calm & informative)

## Phase 4 — PWA
- [x] Full manifest + multi-resolution icons
- [x] Service worker (network-first for API)
- [ ] Test Lighthouse PWA score ≥ 90, installable on Android/desktop

## Phase 5 — Placeholder Feature Cleanup
- [x] Tidy up IPFS evidence flow (kept, presentation cleaned up in history)
- [x] Hide/remove UI trigger for out-of-scope features (AI assistant, live map) or label them clearly "Coming soon" if still shown as roadmap

## Phase 6 — Deployment & Portfolio Polish
- [x] Production-ready config (CORS/API URL/DSN from env)
- [x] Fix lint errors + GetProfile repository refactor
- [x] Remove dead code + migrate AuthModal/ProfileTab tokens
- [ ] Deploy backend (Fly.io/Railway) + frontend (Vercel) + DB (Neon/Supabase)
- [ ] Final README + demo GIF/video
- [ ] Complete `.env.example` without unsafe defaults

## Advanced Polish Phase (font, motion, centered modal, notifications, help center)
- [x] Set up Plus Jakarta Sans font + framer-motion foundation — `layout.tsx` swaps Geist → `Plus_Jakarta_Sans` (weights 400/500/600/700 per DESIGN.md §3), `globals.css` `--font-sans` pointed to `--font-plus-jakarta-sans`, `--font-geist-*` references removed; `framer-motion` installed; shared constants in `lib/motion.ts` (`tapScale`, `hoverScale`, `transitionFast`, `transitionModal`, `fadeSlideUp`) created — not yet applied to components (separate task)
- [x] Fix logout covered by nav + replace emoji greeting with animated icon — shared scroll container ensured `pb-32` (128px ≥ requirement ±111px = BottomNav height ±95px + 16px gap) so the "Keluar Akun" button in `ProfileTab` clears the `BottomNav` on all tabs; greeting in `app/page.tsx` replaced with lucide `Hand` icon in `--color-primary` color size 20 aligned with the name, wave animation rotate `[0,14,-8,14,0]` 0.6s easeInOut using framer-motion, running once on mount (keyframes at module scope so they don't re-trigger on re-render)
- [x] Centered modal/dialog (shared `Dialog.tsx` component) applied to every sheet — created `app/components/ui/Dialog.tsx` (backdrop `bg-black/40` + blur-sm, centered card `flex items-center justify-center`, max-w-md, max-h 90vh + `overflow-y-auto`, fade + scale 0.95→1 animation using `transitionModal` from `lib/motion.ts`, X button top-right, close via backdrop/X/Escape); converted RewardSheet, DropPointSheet, PilahPintarSheet, AuthModal, the OrderHistory detail modal, and ProfileTab's 3 sub-modals (Edit Data/Alamat/Metode Penarikan) from old bottom-sheet/slide-up (fixed bottom, rounded-t-only, drag handle) to centered Dialog — list/form content unchanged, only wrapper positioning. Logic/state (useUIStore, activeModal, selectedOrder) untouched.
- [x] Remove outer device-frame border, full-bleed responsive layout — the "phone box" frame was found in 2 root pages (not `layout.tsx`): `app/page.tsx` & `app/collector/page.tsx` (pattern `w-full max-w-md h-[100dvh] sm:h-[850px] ... sm:rounded-[2.5rem] shadow-2xl border border-neutral-100` + outer wrapper `flex items-center justify-center sm:p-8`). Changed: mobile full-bleed (no max-width/border), desktop `md:max-w-xl md:mx-auto` clearly centered without a frame — main is now transparent `bg` (neutral-50 page blends in), `h-[100dvh]` height kept. BottomNav & absolute elements stay relative to viewport because `main` stays `relative h-[100dvh]`. CDP verification: mobile 390×844 main full-width 390px no frame, nav gap 24px from bottom; desktop 1366 main 576px centered (left 395/right 971), height 900, nav gap 24px.
- [x] In-app notification center (NotificationCenter + store) — 1) `store/useNotificationStore.ts`: Zustand WITHOUT persist (purely derived/transient, intentionally reset between sessions), type `AppNotification {id,title,message,timestamp,read,type}` (type `'status_change'|'system'`), actions `addNotification/markAsRead/markAllAsRead/clearAll`, computed selector `selectUnreadCount`, cap 50 notifications. 2) Data source DERIVED from existing polling in `ActivePickupCard.tsx` (no new polling; collector side skipped because `collector/page.tsx` has no polling, only manual scan): `applyPickup` detects status transitions of the same pickup via `prevActiveRef` → automatic `addNotification` (PENDING→ACCEPTED "Pickup-mu diambil kolektor!", ACCEPTED→VERIFYING "Kolektor sudah menimbang"); COMPLETED transition added in `handleConfirm` with the real karma value (`+X Karma`). TYPE-only verified: COMPLETED transition feedback shows in notifications even when the card unmounts after polling refresh. 3) `app/components/dashboard/NotificationCenter.tsx` wrapped with existing `ui/Dialog.tsx` (not a new modal), timestamp-descending list, each item title+message+relative time (local `timeAgo` helper: Baru saja→minutes→hours→days→date, no date library), unread dot `--color-primary` + bg `primary/5`, "Tandai semua terbaca" button (appears when there are unread), clicking an item→`markAsRead`, empty state follows DESIGN §Empty states (`BellOff` round neutral-100 + short text). 4) Trigger: "Notifikasi & Radar" entry point in `ProfileTab.tsx` connected to `useUIStore.openNotificationCenter`; ADDITIONAL bell icon + unread badge in header `app/page.tsx` aligned with the profile icon (top-right), `<NotificationCenter/>` mounted on root page. 5) Badge: red dot `bg-status-error` absolute at the bell icon corner, unread count, `9+` if >9. Notifications reset on logout/logout-cross-account via `AuthBootstrap` (userData null → `clearAll`). Open/close state added to `useUIStore` (`isNotificationCenterOpen`). `npm run build` success (TS clean) + `npm run lint` 0 errors (one pre-existing `setAddresses` warning in ProfileTab, outside task scope).
- [x] Decision-tree help center (HelpCenter, replaces HelpTab placeholder) — 1) `lib/helpContent.ts`: static data (interface `HelpAnswer`/`HelpCategory`, `helpCategories` 5 categories: pickup/karma/akun/bukti/mitra) — content EXACTLY per spec, `icon` field is a Lucide component name. 2) `app/components/dashboard/HelpCenter.tsx`: new component wrapped with `ui/Dialog.tsx` & `lib/motion.ts` (transitionModal), 3 levels `category→question→answer` with internal level/category/question state, per-level backward navigation (back `< Kategori`, `< [category name]`, "Kembali ke semua kategori" button straight to level 1). Level transitions use framer-motion `AnimatePresence mode="popLayout"` + directional horizontal slide variants (`slideVariants` custom = moveDir): enter from right on drill-down, exit right on back — not instant content swap. Category icons via `ICON_MAP` string→component (fallback `HelpCircle`). Level-1 search box DONE: filters categories + questions by text, clear button & empty state present. Full state reset when dialog closed (handleClose). Anti-slop: solid Lucide icons, neutral, no "Pilah AI"/Sparkles (HelpTab placeholder & AI brand removed). 3) Trigger: the 3rd BottomNav `help tab` in `page.tsx` intercepted (`onTabChange`) → `openHelpCenter()` (no longer switches tab), HelpTab import removed & `help` render branch removed; "Pusat Bantuan" entry in `ProfileTab.tsx` → `openHelpCenter()`. `HelpTab.tsx` (placeholder "Segera Hadir") DELETED. `useUIStore` gains `isHelpCenterOpen/open/close`. `npm run build` success (TS clean) + `npm run lint` 0 errors.
- [x] Auto-cancel PENDING pickup after 3 minutes + retry — BACKEND (`backend`): polling endpoint `GET /api/v1/pickups/history` → `handlers.GetUserHistory` → `services.pickupService.GetUserHistory`. Constant `PickupExpiryDuration = 3 * time.Minute` defined once in `services/pickup_service.go` (not a magic number). Lazy auto-cancel: before history is returned, `cancelExpiredPendingPickups(userID)` cancels all the user's PENDING pickups where `created_at < now - expiry`, via `repo.CancelExpiredPendingPickups` (bulk UPDATE `WHERE user_id=? AND status='PENDING' AND created_at<?` in one simple transaction — the WHERE protects the race with AcceptPickup). `CANCELLED` status is safe: the model is just a `default:'PENDING'` string without a DB CHECK constraint, and all processing paths (Accept/FindByID*) already have status-specific WHERE clauses so a cancelled pickup can't be accepted/processed. FRONTEND (`frontend`): `ActivePickupCard.tsx` handles `CANCELLED` as a terminal state — when the active pickup disappears & the latest record is CANCELLED, the stepper isn't rendered; replaced with an `XCircle` card in `--color-status-error` + informal message "Yah, orderan kamu dibatalkan otomatis... Belum ada kolektor yang gercep... dibatalkan setelah 3 menit..." + "Coba Lagi" button that triggers the existing create flow (`onRetry={handleJemput}` from `app/page.tsx`, reuses logic, doesn't create new) — old card gone, normal new flow; polling stopped while the CANCELLED card shows (terminal, like COMPLETED) and resumed after retry. `OrderHistory.tsx`: CANCELLED badge added (XCircle icon + "Jemputan Dibatalkan" text + `--color-status-error` color in list & detail modal; GetUserHistory query already returns all statuses including CANCELLED, no query change needed). `go build` OK, `go vet` OK, `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning). 3-minute testing can be sped up: temporarily shrink `PickupExpiryDuration` (e.g. `5 * time.Second`), test, restore to `3 * time.Minute`.
- [x] Help center becomes floating icon (HelpFAB), BottomNav 3 tabs — help center entry point moved from the BottomNav tab to the FAB. `BottomNav.tsx`: "help/Bantuan" tab removed → 3 tabs (Beranda/Pesanan/Profil), `flex justify-between` layout in the glassmorphism pill stays centered & proportional automatically. `app/components/dashboard/HelpFAB.tsx` (new): round button `w-14 h-14` (56px), `fixed` bottom-right `right-4` (16px), bottom position `calc(var(--bottom-nav-height, 128px) + 20px)` using the existing BottomNav size CSS variable → ABOVE the nav, not covering it; `MessageCircleQuestion` (lucide) icon white on `--color-primary`, `shadow-soft`, `whileHover={hoverScale}` & `whileTap={tapScale}` animations from `lib/motion.ts` + `transitionFast`; `z-40` (above content z-10 & nav z-30, below Dialog z-[70]); onClick → `openHelpCenter()` from `useUIStore` (existing state, not new), opens the existing `HelpCenter` without changing its decision-tree logic. Mounted only on the warga side (`app/page.tsx`); the old "help" tab intercept in `page.tsx` removed entirely (`onTabChange={setActiveTab}`), `openHelpCenter` no longer destructured in the page. The "Pusat Bantuan" entry in `ProfileTab.tsx` REMAINS as an alternative access. `npm run build` success (TS clean) + `npm run lint` 0 errors (1 pre-existing `setAddresses` warning).
- [x] Fix test collector account (id 20) + add role option at registration — **ROOT CAUSE of "Akses ditolak untuk role ini"**: NOT a middleware bug. All DB accounts have role `'user'` (default in model `models/user.go`), none is `'collector'`, and there's no path to create a collector account — `RegisterRequest` (`handlers/auth.go`) only has `name/email/password`, the handler doesn't set role, and frontend `registerUser` & `AuthModal.tsx` also don't send role. So the JWT (built from `user.Role` in `auth_services.go`) always contains `role:"user"`, and `RequireRole("collector")` rejects it. The `Protected()`/`RequireRole` middleware + `.(string)` type assertion are already correct. **Task 1 (data)**: `UPDATE users SET role='collector' WHERE id=20` (rahel/ghifariakbar2006@gmail.com) → exact `'collector'` value (role_len 9). Ghif must log out & log back in on the browser so the new token carries the `role:"collector"` claim. **Task 2 (root cause, code)**: BACKEND `handlers/auth.go` — `RegisterRequest` + `role` field; `RegisterUser` whitelist validate via helper `normalizeRole`: empty→default `'user'`, anything other than `"user"`/`"collector"`→400 Bad Request (don't trust client input). FRONTEND `lib/api.ts` — `registerUser(name,email,password,role)` sends `role`; `AuthModal.tsx` — register form in non-login mode adds a 2-button toggle "Daftar sebagai Warga"/"Mitra" (`background/ring` using `--color-primary`, `UserIcon`/`Truck` lucide icons, no illustrations/stickers) + short context text per choice, `role` state submitted to backend. No role-based auto-redirect logic in the app (both pages static) → registration flow stays on the same page, Mitra users can continue manually to /collector. Endpoint verification: role=`collector`→201 & role saved, role=`admin`→400 whitelist, no role→201 default `user` (test user cleaned up after). `go build` OK, `go vet` OK, `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning).
- [x] Fix ActivePickupCard state management (completed & cancelled don't clear/overlap) — **ROOT CAUSE**: (1) 2 separate states (`pickup` for active + `cancelledPickup` for CANCELLED) & NO stale response guard — polling is triggered from many overlapping sources (mount/identity effect + `setInterval` 5s + explicit `await pollActive()` after confirm), a `getUserHistory` response snapshotting an OLD status can resolve AFTER a change and overwrite the new state (`setPickup(staleActive)` + `setCancelledPickup(null)`) → old card resurrected: Symptom A = stale VERIFYING response arrives after confirm → "Mencari Kolektor/Menunggu" card reappears above the success banner "Selesai! +X Karma" (`feedback` never cleared in `applyPoll`); Symptom B = stale PENDING response arrives after auto-cancel → overwrites the cancelled card, old card shows again. (2) NO terminal COMPLETED branch (only `setPickup(null)`) + no dismiss mechanism. **FIX** (`ActivePickupCard.tsx`): ONE `pickup` source of truth (any status) + TOTAL replace in `applyPoll` (not partial merge); `pollSeqRef` guard (each poll increments ++counter, responses with seq ≠ latest are DISCARDED) prevents stale overwrite; `terminalRef` stops polling when a terminal card shows; `dismissedRef` prevents a COMPLETED the user already closed from being resurrected by polling; `feedback` cleared when pickup changes/`null`; render = 1 clear switch: PENDING/ACCEPTED/VERIFYING→stepper, COMPLETED→success card (stepper "Selesai" all checks + ShieldCheck + karma banner + **Tutup** button that clears `pickup`→back to ServiceGrid), CANCELLED→cancelled card + Coba Lagi. Chose MANUAL dismiss (not auto-dismiss): so the user has time to read the karma amount, also consistent with the cancelled card that needs an explicit action (Coba Lagi). E2E verification (API+CDP 390×844, backend with constant sped up to 5s for CANCELLED then restored to 3m): **COMPLETED** flow create→accept→complete→confirm → success card shows (stepper "Selesai", no "Mencari Kolektor", `+50 Karma` banner when confirming from the card, Tutup button), click Tutup → card gone & back to ServiceGrid; **CANCELLED** flow create→expire → cancelled card + Coba Lagi shows CORRECTLY (not the old card), stable after +6s (no flapping back — stale guard works). `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning).

- [x] Fix WaitingRadar appearing again after dismissing the Selesai card — **ROOT CAUSE (EXACT)**: `WaitingRadar` ("Mencari Pahlawan...") is rendered based on `orderStatus.type === 'success'` (`app/page.tsx`), and **`orderStatus` can only return to `'idle'` via the "Batalkan Pencarian" button** (`handleCancelRadar`). `isSearching` (`useUIStore`) is NEVER connected to WaitingRadar — only used to disable+spinner the "Jemput" button in `ServiceGrid` (and `startSearching`/`stopSearching` ARE already always balanced: `stopSearching()` is always called in the `finally`/error path of `handleJemput`). So the flag that gets STUCK isn't `isSearching` but **`orderStatus`**: after `createPickupRequest` succeeds (page:55) it stays `'success'` forever — not reset when a PENDING pickup starts showing, when confirming COMPLETED, nor by the **Tutup** button (from the previous fix) which only does `setPickup(null)` in ActivePickupCard. Result: once the COMPLETED card is hidden, WaitingRadar shows again. **FIX** (`app/page.tsx` + `ActivePickupCard.tsx`): (1) `ActivePickupCard` exposes an `onPickupVisibleChange(visible)` callback sent via `useEffect([pickup])` — the page uses this as the SINGLE source-of-truth render driver: as soon as ANY pickup card (active/terminal) starts showing, `handlePickupVisibleChange` resets `orderStatus→'idle'` and also `stopSearching()` (via `useUIStore.getState()`). Consequence: WaitingRadar ONLY shows when "request sent but no pickup card showing yet" — automatically disappears once a PENDING card appears, and CANNOT reappear after Tutup (orderStatus is already idle). (2) the **Tutup** (COMPLETED) & **Coba Lagi** (CANCELLED) buttons also call `useUIStore.getState().stopSearching()` as part of full state clear. (3) `page.tsx` adds `hasActivePickup` state (set from the same callback) so `ServiceGrid` stays disabled while a pickup is active (prevent double-submit), not only during radar. `npm run build` success (TS clean) + `npm run lint` 0 errors (1 pre-existing `setAddresses` warning). Manual E2E verification (API+CDP 390×844, register new warga + collector `mitra@pilah.test`, CDP geolocation emulation `Emulation.setGeolocationOverride`): click Jemput → WaitingRadar shows (true) → collector accept+complete → VERIFYING card shows in UI (true) → confirm → COMPLETED card + Tutup button (true) → click Tutup → 3s later `completedGone=true, serviceGridBack=true, radarNow=false, radarCancelBtn=false` → **watch 10 seconds every 500ms: radar never reappears (false/NO)** → final: `radar=false, gridBack=true`.

- [x] Weight-based karma formula (60/24/6 per kg) — replace flat `+50 Karma` with dynamic calculation in `backend/internal/services/pickup_service.go`. Clear constants at the top of the file: `KarmaPerKgPlastik=60`, `KarmaPerKgKardus=24`, `KarmaPerKgKaca=6` (per PRD: "Perhitungan poin berbasis berat per material"). Helper `calculateKarma(p models.Pickup) int` = `math.Round(plastic*60 + cardboard*24 + glass*6)` (import `math`), replacing `const karmaGained = 50` in `ConfirmPickup` — Go constants can't call functions, so it's computed after the pickup is loaded inside the transaction (`karmaGained := calculateKarma(*pickup)`). **Fallback choices (clear)**: (a) total weight > 0 but rounding result < 1 (e.g. glass 0.04 kg → 0.24 → round 0) → floor `minKarmaForValidPickup=1` so a valid transaction doesn't award 0 karma; (b) all weights 0 (old/empty data) → `fallbackKarmaOldData=5` as a base reward. No DB schema change (purely calculation logic). IPFS evidence that was hardcoded `"+50 Karma"` also fixed: `pinPickupToIPFS(repo, pickup, karma)` uses the actual value `"+<karma> Karma"` (import `strconv`). Frontend GUARANTEED no change needed: `confirmPickup()` (`lib/api.ts`) already takes dynamic `karma` from the response `{ pickup_id, karma }`, and `ActivePickupCard.tsx` displays `+${res.karma} Karma`. `go build` OK + `go vet` OK. API verification (register new warga + collector `mitra@pilah.test`): input plastic 2 kg + cardboard 1 kg + glass 0.5 kg → **response karma 147** = 2×60 + 1×24 + 0.5×6 ✓ (user karma_points 0 → 147, delta matches). Edge cases: all weights 0 → **5** ✓; only glass 0.04 kg → **1** ✓. UI verification (CDP 390×844, click "Konfirmasi Selesai" button directly in browser): feedback shows **"+147 Karma"** + COMPLETED card ✓, no stale "+50" text.
- [x] Warga weight estimate (EstimateWeightDialog) + shown in collector radar — optional feature: warga can input estimated weight (plastic/cardboard/glass kg) in a separate step AFTER clicking "Jemput Sampah" and BEFORE the GPS request is sent. The estimate is ONLY a reference for collectors on the radar — **not the final karma source** (karma is still calculated from the actual weight the collector submits in `ConfirmPickup`/`calculateKarma`, previous task). **BACKEND**: (1) `models/pickup.go` adds 3 columns `EstPlasticWeight/EstCardboardWeight/EstGlassWeight` (float64, `gorm:"default:0"`, JSON tag `est_*_weight`) — placed before the actual `PlasticWeight` so they're clearly separate as estimates; (2) new migration `backend/migrations/0003_add_pickup_estimation_fields.sql` (3 `ADD COLUMN IF NOT EXISTS ... double precision NOT NULL DEFAULT 0`) — not auto-run, AutoMigrate (`config/database.go:52`) handles dev like the previous pattern; (3) handler `CreatePickup` (`handlers/pickup.go`) accepts 3 optional body fields (default 0 if not sent) and passes them to the service; service interface + `CreatePickup(userID, lat, lng, estPlastic, estCardboard, estGlass)` saves them to the new record; (4) `GetPendingPickups` (radar) automatically returns the 3 fields via JSON tag (verified). **FRONTEND**: (5) `components/dashboard/EstimateWeightDialog.tsx` (new) — wrapped with existing `ui/Dialog.tsx`: 3 number inputs (Plastik/Kardus/Kaca, optional, min 0, step 0.1, `kg` suffix format, number spinner removed via inputmode decimal), informal explanation text "Estimasi aja, gak perlu presisi — kolektor bakal timbang ulang pas di lokasi." (`Scale` icon), **"Lanjut"** button → `onContinue(WeightEstimate)` with values (empty→0), **"Lewati"** button → `onSkip` continues with all 0 (does NOT force filling); inputs reset each time the dialog opens/closes; (6) `app/page.tsx` — `handleJemput` is now two-phase: click Jemput → `setIsEstimateOpen(true)` (NOT directly GPS); `proceedWithEstimate(estimate)` saves to `pendingEstimateRef` then `sendPickupRequest()` = existing GPS+create flow, also sends the estimate (`createPickupRequest(lat, lng, estimate)`); `skipEstimate()` = continue with 0; `<EstimateWeightDialog/>` mounted at root page; (7) `lib/api.ts` — `Pickup` interface + `est_*_weight`; `createPickupRequest(lat, lng, estimate?)` sends the 3 fields (only if `!== undefined`); (8) `collector/page.tsx` — helper `buildEstimateChips(pickup)` builds chips `~2kg Plastik`/`~1kg Kardus`/`~0.5kg Kaca` ONLY for materials > 0 (if all 0 → empty array, NO placeholder — DESIGN.md empty-state principle), rendered as small rounded-full badges in radar items. `go build` OK + `go vet` OK + `npm run build` success (TS clean) + `npm run lint` 0 errors (1 pre-existing `setAddresses` warning). API verification: CASE A (estimate 2/1/0.5 sent) → radar `est: 2/1/0.5` ✓; CASE B (skip, fields not sent) → radar `0/0/0` ✓; CASE C (explicit 0) → `0/0/0` ✓; `has est keys in response: true`. UI verification (CDP 390×844): click Jemput → "Estimasi Berat Sampah" dialog opens + explanation text present ✓; fill plastic 2 + cardboard 1 → click Lanjut → GPS (emulation) → WaitingRadar appears (create success) ✓; on collector side click Scan → radar badge **"~2kg Plastik"** appears ✓.

- [x] Mitra profile saved to backend & editable (replace hardcoded data + remove fake rating) — **FINDING**: (1) the collector profile tab (`collector/page.tsx`) shows **HARDCODED** data "Motor Bak / Jakarta Pusat / BCA ****1234" + fake **4.9** rating; (2) the warga "Edit Data Diri" in `ProfileTab.tsx` only does `setUserData` (local Zustand state, never sent to backend). **BACKEND**: `models/user.go` adds `VehicleType/ServiceArea/BankName/BankAccountNumber` (string `gorm:"size:50/100;default:''"`, JSON `vehicle_type/service_area/bank_name/bank_account_number`) so warga (user role) aren't affected; migration `0004_add_user_profile_fields.sql` (4 `ADD COLUMN IF NOT EXISTS ... varchar NOT NULL DEFAULT ''`; dev handled by AutoMigrate `config/database.go:52`); `user_repo.go` — `GetByID` Select widened from `id/name/email/karma_points/role` to +4 profile columns (password still never selected) + new method `UpdateProfile(id, updates map[string]interface{})` (GORM `Updates` with a map = partial update); `auth_services.go` — interface + `UpdateProfile(userID, ProfileUpdate) (*models.User, error)`: build the map only from non-empty fields (`Name/VehicleType/ServiceArea/BankName/BankAccountNumber`), if all empty → error (no partial update that overwrites data), then `repo.UpdateProfile` + return latest `GetByID`; `handlers/auth.go` — `UpdateProfileRequest` (5 fields), handler `UpdateProfile`: user_id from `c.Locals("user_id")` (JWT, NOT from body — prevents editing another account), `strings.TrimSpace` on each field, error → 400, success → `{status,data:user}`; `main.go` — new route `api.Patch("/users/me", middleware.Protected(), handlers.UpdateProfile)` (CORS AllowMethods already includes PATCH, no CORS change). **FRONTEND**: `lib/api.ts` — `UserProfile` + 4 optional fields, `ProfileUpdatePayload` + `updateProfile(payload)` (PATCH `/users/me`, `credentials:include`); `store/useAuthStore.ts` — `UserData` + 4 optional fields; `ProfileTab.tsx` — `saveProfile` now async → `updateProfile({name})`, `setUserData` from the response, error → "Gagal menyimpan" toast; `collector/page.tsx` — Profile tab: vehicle/area/account rows from `userData` (empty → honest "Belum diatur", not dummy), "4.9" rating REMOVED → `<CheckCircle2/>` + `{completedMissions} Pickup Selesai` badge (from `getCollectorHistory()`, history effect widened to the profile tab so the badge counts without opening Riwayat), **"Edit Profil Mitra"** button (`Pencil` icon, primary color) → `ui/Dialog.tsx` dialog: 4 inputs (Jenis Kendaraan/Area Operasional + Nama Bank/No. Rekening in a 2-column grid) with correct placeholders, "Simpan Perubahan" button → `saveProfileMitra`: `updateProfile` → `setUserData` (merging profile fields) → close dialog + success banner; errors via banner. **Verification**: `go build ./...` OK, `go vet ./...` OK, `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning). API (`test_profile_api.mjs`, mitra `mitra@pilah.test`) — login → PATCH set all 4 fields → persisted in next GET; PATCH with only `name` → other fields UNCHANGED (partial update correct); empty/whitespace body → 400. UI CDP 390×844 (`verify_profil_ui.mjs` + debug_profil.mjs): Edit Profil Mitra dialog opens, change vehicle → "Profil mitra diperbarui!" banner + row updates live + after `Page.reload` value persists; DB confirmed directly via API (vehicle_type="Motor Bak"). Warga (`verify_profil_warga.mjs`): Edit Data Diri opens with the default name from userData, save → "Profil berhasil diperbarui!" toast + header name updates live + reload persists (same endpoint, name-only PATCH verified in the API test). Note: initial test scripts misread due to CSS-uppercased innerText for badges/labels (`7 PICKUP SELESAI`, `NO. REKENING`) and the two-level API envelope (`data.data`), not an app bug.

- [x] Fix the Selesai card reappearing after switching tab (dismissedRef → global store) — **ROOT CAUSE (EXACTLY as suspected)**: (1) `renderContent()` in `app/page.tsx` only renders `<ActivePickupCard/>` in the default/home branch (lines 116-140) — when `activeTab === 'orders' || 'profile'` the component **fully unmounts**; (2) `dismissedRef` in `ActivePickupCard.tsx` is a LOCAL `useRef` (line 76, `useRef<string | null>(null)`) — its value is LOST every time the component unmounts/remounts. Result: user closes the COMPLETED card → `dismissedRef` filled with the id → switches tab (ref & polling dead, interval cleared) → back Home → component remounts, `dismissedRef` back to `null`, polling runs again, history returns the latest COMPLETED pickup → `applyPoll` has no "memory" of the dismissal → the Selesai card shows again. **FIX**: (3) `store/useUIStore.ts` adds global state `dismissedPickupId: string | null` + `setDismissedPickupId(id)` (NOT persisted to localStorage — this store genuinely has no persist; resetting on refresh is fine; it only needs to last the browser session). (4) `ActivePickupCard.tsx` — `dismissedRef` REMOVED; all uses replaced with read/write `useUIStore.getState()`: `handleDismissCompleted` → `setDismissedPickupId(pickup.id)`, branch `if (active)` → `setDismissedPickupId(null)` (a new pickup appearing = old dismissal irrelevant), branch `if (terminal)` → `if (terminal.status === 'COMPLETED' && useUIStore.getState().dismissedPickupId === terminal.id)` skip. Read/write imperatively via `getState()` (not hooks) inside `applyPoll` (useCallback dep `[detectStatusChange]`) to avoid stale closures — consistent with the already-in-use `stopSearching()` pattern. (5) Other logic UNCHANGED (closed COMPLETED + polling forbidden to resurrect, same as before). (6) **Other refs LEFT local + why**: `terminalRef` — resetting on remount is actually desired (polling MUST resume after returning to the tab; thanks to `dismissedPickupId` in the store, that polling won't resurrect the Selesai card). `retryingRef` — if it resets "early" (tab switch mid Coba Lagi flow) it only re-shows the CANCELLED card, which is the true status; once a new pickup appears, `applyPoll` clears it. `pollSeqRef` — stale responses can't cross instances (old closure destroyed on unmount, interval cleared), seq resets then recounts from 0 without conflict. `prevActiveRef` — purely notification dedup; a reset only risks missing/duplicating ONE notification, doesn't resurrect a card. **Verification**: `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning). Manual E2E test (CDP 390×844, `verify_dismiss_tab.mjs`): register new warga → create pickup via API → accept/complete/confirm (all 200) → COMPLETED card shows → click **Tutup** → card gone (watch 6.5s) → switch to **Profil** tab → back **Beranda** (watch 13s = ±2 polling cycles after remount) → card does NOT reappear ✓ → repeat **Pesanan** tab → card does NOT reappear ✓. Positive control (`verify_dismiss_control.mjs`, valid methodology): second COMPLETED pickup WITHOUT dismiss → switch tab → back → card DOES reappear (proves the resurrect mechanism is real & remount polling stays alive) — suppression is purely from `dismissedPickupId` in the store, not broken polling.
- [x] Framer-motion micro-interactions on the collector side (consistent with the warga side) — `frontend/app/collector/page.tsx` (ONLY file changed, logic/state UNTOUCHED) keeps the existing constants in `lib/motion.ts` (`tapScale 0.97`, `hoverScale 1.02`, `transitionFast 0.15s`, `fadeSlideUp`, `transitionModal`), adds `AnimatePresence, motion` import at the top. Animated elements: (1) **Map pins** (waste-point markers, `!activeOrder` condition) → wrapped in `AnimatePresence`, each pin becomes a `motion.button` with **OPACITY-ONLY fade-in/out** (`initial/animate/exit opacity`, `transitionFast`) — deliberately NOT using transform/scale/hover on pins because framer's inline transform would override the Tailwind `-translate-x-1/2 -translate-y-full` classes that position them (the pin "hover/highlight" effect stays via CSS `hover:scale-105`/`scale-110` in classes, no conflict); (2) **Pickup point list (radar)** → each item becomes a `motion.div` with `{...fadeSlideUp}` (enter: opacity 0→1 + y 8→0) + `exit={{opacity:0, y:-8, transition:transitionFast}}` + `whileHover={hoverScale}` → appears & disappears smoothly as scan turns on/order accepted; (3) **Trip history list (Riwayat tab)** → items become the same `motion.div` (fadeSlideUp + exit + whileHover); (4) **CTA buttons** → become `motion.button` with `whileTap={tapScale}` + `transition={transitionFast}`, the CSS class `active:scale-[0.98]` REMOVED (avoid double-transform, framer inline wins): "Konfirmasi Selesai", "Ambil Tugas Ini", "Scan Area", "Edit Profil Mitra", "Akhiri Shift", "Simpan Perubahan" (profile dialog), "Masuk Sekarang" ("Belum masuk sebagai mitra" block); (5) **"Edit Profil Mitra" dialog** — NOT modified because it already uses `app/components/ui/Dialog.tsx` (import line 10), which since the dialog-centering task is already animated with `AnimatePresence` + fade + scale 0.95→1 `transitionModal` (re-verified `Dialog.tsx` contains motion). `npm run build` success (TS clean) + `npm run lint` 0 errors (1 pre-existing `setAddresses` warning in ProfileTab, not from this task). No new motion constants added, no change to polling/scan/accept/complete/logout logic.

- [x] Functional Tukar Cuan (real backend karma redeem + simulated payout) — **BACKEND**: new endpoint `POST /api/v1/karma/redeem` (`Protected()` middleware, in `main.go` after the `/users/me` route). `handlers/auth.go` — `RedeemKarmaRequest{amount_karma int}` + handler `RedeemKarma`: user_id from JWT `c.Locals("user_id")` (not body), request <= 0 → 400, success → `{status, data:{new_balance, rupiah_value: amount* services.KarmaToRupiahRate}}`. `services/auth_services.go` — constant `KarmaToRupiahRate = 50` (simulated rate 1 karma = Rp50, the single source of the rate) + interface & impl `RedeemKarma(userID, amountKarma) (newBalance int, err error)`: validate `amountKarma <= 0` → error, then a real transaction: `repo.Begin()` → `repo.GetUserByIDForUpdate(tx, userID)` (**FOR UPDATE row lock** in the repo, prevents two-redeem race) → if `user.KarmaPoints < amountKarma` → rollback + exact error **"Karma kamu gak cukup buat jumlah segini"** → `repo.DeductKarma(tx, userID, amountKarma)` (GORM `karma_points - ?`) → commit. `repository/user_repo.go` — interface + `Begin()`, `GetUserByIDForUpdate(tx, ...)` (Clauses Locking UPDATE), `DeductKarma(tx, ...)`. **FRONTEND**: `store/usePaymentMethodStore.ts` (NEW) — `paymentMethods/selectedMethodId/addPaymentMethod/setSelectedMethodId`, **persisted to localStorage** (`pilah-payment-methods`) because it's only a UI preference (not real financial data), defaults GoPay + Bank Transfer. `lib/api.ts` — `RedeemKarmaResponse{new_balance, rupiah_value}` + `redeemKarma(amountKarma)` (POST `/karma/redeem`). `ProfileTab.tsx` — local `paymentMethods` state REMOVED, refactored to use `usePaymentMethodStore` (additions here are automatically read by RewardSheet; no visual/modal-flow change). `components/dashboard/RewardSheet.tsx` — FULL replace of the "Segera Hadir" placeholder: (1) dark balance card (active karma + "Senilai Rp X" using `toLocaleString('id-ID')` + note "Kurs 1 Karma = Rp50"); (2) **amount choice = "Tarik Semua Karma" button** (FULL BALANCE approach, not a number input — simpler & no validation edge-cases; card always selectable, shows `X Karma = Rp Y`); (3) payout method radio from the store (Smartphone/CreditCard icon per kind, selected → border-primary); (4) **"Tarik Sekarang"** button `motion.button` `whileTap` + `disabled={karma<=0 || !selectedMethod}` + small hint "Karma masih 0..." when empty; (5) flow: click → `redeemKarma(karma)` REAL first (backend deducts balance) → `setTimeout` 1600ms (artificial delay) → `getUserProfile()` + `setUserData()` (refresh userData → KarmaWallet goes to 0 too) → success screen `ShieldCheck` green `--color-status-completed`, **"Dana Rp X telah dikirim ke GoPay"** + REQUIRED small disclaimer `text-xs text-neutral-400`: "Simulasi untuk keperluan portofolio — dana tidak benar-benar terkirim." + Selesai button; (6) phase machine `idle|processing|success` wrapped in `AnimatePresence mode="wait"` + `motion.div` (processing: `Loader2 animate-spin` + "Memproses penarikan..."), reset to idle via `handleClose` (not useEffect — passes lint `react-hooks/set-state-in-effect`); (7) Dialog.tsx + `lib/motion.ts` constants USED, no new animation/modal from scratch. Manual verification (API + CDP 390×844, fresh user karma set to 147 via DB): **DB before = 147** → redeem amount 999 → 400 "Karma kamu gak cukup buat jumlah segini" & DB STILL 147 → UI open Tukar Cuan shows "Saldo Karma Aktif 147 / Senilai Rp 7.350" + "Tarik Semua Karma 147 Karma = Rp 7.350" → Tarik Sekarang → "Memproses penarikan..." screen (spinner, ~1.6s) → "Penarikan Berhasil! Dana Rp 7.350 telah dikirim ke GoPay" + disclaimer shows → **DB after = 0** (REALLY deducted, not just UI) → browser refresh → Saldo Karma 0 + "Tidak ada karma untuk ditarik" + `GET /users/me` karma_points 0 (proof it's from backend, not cache). Test user cleaned up. `go build` OK + `go vet` OK, `npm run build` success (TS clean) + `npm run lint` 0 errors (1 pre-existing `setAddresses` warning in ProfileTab).

- [x] Fix the "Keluar Akun" button in ProfileTab that was STILL covered by BottomNav (even though pb-32 was once applied)
  - The actual root cause: root `ProfileTab` uses `h-full` -> box locked to viewport height, logout content overflow slips down into the nav area so the padding clearance in the scroll container was ineffective (this is where the manual estimate misled). `h-full` removed.
  - Dynamic solution: `BottomNav` measures its real height via ref + getBoundingClientRect (fallback to ResizeObserver/resize/orientationchange when 100dvh changes) then exposes the CSS var `--bottom-nav-height` at the root. The scroll container uses `padding-bottom: calc(var(--bottom-nav-height, 128px) + 16px)`.
  - Dashboard greeting becomes plain text "Halo, {nama}!" (`Hand` icon + wave animation removed, per DESIGN.md anti-AI-slop).
  - MEASURED (not calculated) verification: headless Edge 390x844 + real login -> Profil tab -> max scroll: logout bottom=305, nav top=354, clearance=49px, `visibleOk=true`. Screenshot at `%TEMP%\opencode\profil_verify.png`.
  - `npm run build` production: success, TS clean.

- [x] Update FAQ content for entry `k2` (karma category) in `frontend/lib/helpContent.ts` — remove stale "Segera Hadir" text (the Tukar Karma placeholder was replaced with functional Tukar Cuan, previous task), replace with this exact new answer: "Karma kamu bisa langsung ditukar jadi saldo lewat menu Tukar Cuan di halaman utama — tinggal pilih metode pembayaran (GoPay, DANA, bank, atau apa pun yang kamu tambahkan sendiri), lalu tarik semua karma sekaligus. 1 karma setara Rp50." Other FAQ entries unchanged. `npm run build` success (TS clean).

- [x] Fix collector weight input form (root cause of 0kg breakdown) — **EXACT ROOT CAUSE**: `handleComplete` in `collector/page.tsx` calls `completePickup(activeOrder.id.toString())` WITHOUT a body — there was never a weight form/input on the collector side. Backend `CompletePickup` (handler `pickup.go` + service `pickup_service.go`) indeed ALREADY accepts body `PickupVerification` (plastic/cardboard/glass/photo), but because the body was empty (`len(c.Body())==0`) all weights saved as 0 → warga breakdown always 0kg during VERIFYING & karma used the fallback. **FIX**: create `frontend/app/components/dashboard/CollectorCompleteDialog.tsx` (new) — existing Dialog.tsx + motion, consistent with `EstimateWeightDialog` but REQUIRES filling: 3 number inputs (Plastik/Kardus/Kaca, step 0.1, min 0), total-weight > 0 validation with the message "Isi minimal 1 jenis berat sebelum menyelesaikan", "Kirim & Selesaikan" button calling `completePickup(id, {plastic_weight, cardboard_weight, glass_weight})` with actual values. `collector/page.tsx` — "Konfirmasi Selesai" button now opens the dialog (instead of calling the API directly), `handleComplete` (direct API without data) removed, new `submitVerification` sends weight data, `isCompleteOpen` state. **PHOTO EVIDENCE GAP (reported, NOT done)**: backend stores `photo_url` string but there's NO image upload endpoint & photo form — not enforced (separate scope). `go build` OK, `go vet` OK, `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning).

- [x] Final audit fix: dynamic karma/earnings in history (A+B) + cleanup (C+D) — **APPROACH (A+B)**: RECOMPUTE from stored weights, NOT using new columns/migration. Backend is the single source of truth: `models/pickup.go` adds transient fields `KarmaEarned int json:"karma_earned" gorm:"-"` & `EarningsEarned int64 json:"earnings_earned" gorm:"-"` (not persisted), filled on-the-fly by `fillEarnedValues()` (service `pickup_service.go`) for every COMPLETED pickup returned by `GetUserHistory` & `GetCollectorHistory` — using the exact same `calculateKarma` (60/24/6 per kg) & `calculateEarnings` (3000/1200/300 per kg) as ConfirmPickup, so history numbers always line up. **FIX A**: `OrderHistory.tsx:87` hardcode `'+50 Karma'` → `+${order.karma_earned ?? 0} Karma`. **FIX B**: `collector/page.tsx` `totalKarma = completedMissions*25` & `'+25 Karma'` → total Rupiah earnings (`totalEarnings = Σ earnings_earned completed`) + item label `Rp {earnings_earned}` & "Total Pendapatan" card `Rp {totalEarnings}` (chose per-transaction earnings, more informative than the `collector_earnings` balance for history stats). **FIX C**: delete `frontend/store/usePickupStore.ts` (dead store, confirmed not imported anywhere). **FIX D**: remove the duplicate `CancelExpiredPendingPickups` comment in `repository/pickup_repo.go`. `go build` OK, `go vet` OK, `npm run build` success (TS clean), `npm run lint` 0 errors (1 pre-existing `setAddresses` warning). API verification full flow 1/1/1 kg: warga `karma_earned=90`, collector `earnings_earned=4500`.

- [x] Comment sweep: English-only, prune noise, keep meaningful docs — `go build`/`go vet` OK, `npm run build` success (TS clean), `npx tsc --noEmit` OK. Indonesian comments → English, restating/divider/redundant comments removed, WHY/race/godoc/JSDoc comments kept. Note: `gofmt -l` still flags some files — that's PRE-EXISTING struct-field misalignment & missing trailing newline (not from the sweep); `gofmt -w` deliberately NOT run because it would change code line alignment (violating the comments-only constraint).

---
**File usage note:** update a checkbox each time a task is done *and* manually verified (see RULES.md). Don't tick just because "the code was written".
