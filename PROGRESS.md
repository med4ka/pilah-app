# PROGRESS.md — Pilah Rework Tracker

Status terakhir diupdate: **Phase 2 — store refactor selesai** (auth/ui/pickup split)

## Phase 0 — Fondasi Dokumen
- [x] PRD.md
- [x] DESIGN.md
- [x] SYSTEM.md
- [x] ARCHITECTURE.md
- [x] RULES.md
- [x] PROGRESS.md (dokumen ini)

## Phase 1 — Security & Architecture Fix (Backend)
- [x] Hapus fallback JWT secret hardcoded, wajib dari env (fail-fast)
- [x] Tambah ownership check di `UserConfirmPickup` (`pickup.UserID == current_user`)
- [x] Tambah ownership check di `CompletePickup` (`pickup.CollectorID == current_user`)
- [x] Perbaiki `AcceptPickup` dengan row lock (`FOR UPDATE`) untuk cegah race condition dua kolektor accept bersamaan
- [x] Bungkus update status + karma dalam satu transaction yang benar (commit/rollback jelas)
- [x] Pindahkan semua query langsung dari `handlers/pickup.go` & `handlers/collector.go` ke repository layer
- [x] Tambah middleware `RequireRole`
- [x] Migrasi auth dari `Authorization` header → httpOnly cookie
- [~] Tambah index `pickups(status, created_at)` — file `backend/migrations/0001_add_pickup_status_index.sql` sudah dibuat, **belum dijalankan** (menunggu Ghif jalankan manual di pgAdmin/psql)

## Phase 2 — Frontend Refactor
- [x] Pecah `usePilahStore` jadi slice: auth / ui / pickup (`useAuthStore`, `useUIStore`, `usePickupStore` placeholder; file lama dihapus)
- [x] Hapus token dari localStorage/persist, ganti deteksi via `GET /users/me`
- [x] Refactor `lib/api.ts` jadi fetch wrapper dengan `credentials: include`
- [x] Audit semua komponen di `app/components/dashboard` — inventarisasi mana yang fungsional vs placeholder murni

## Task tambahan: UI konfirmasi warga (blocker ditemukan saat audit)
- [x] Backend: lifecycle pickup jadi `PENDING → ACCEPTED → VERIFYING → COMPLETED` — `CompletePickup` (kolektor) kini set VERIFYING + simpan berat per material & foto bukti (tanpa karma), `ConfirmPickup` (warga) baru pindahkan ke COMPLETED + transfer karma dan response membawa `karma`
- [x] Frontend: `confirmPickup()` di `lib/api.ts` + komponen `ActivePickupCard` (polling status tiap 5 detik, tampilkan breakdown berat plastik/kardus/kaca + foto bukti, tombol "Konfirmasi Selesai" HANYA saat status VERIFYING, feedback karma setelah confirm)
- [x] Cleanup placeholder (tanpa fitur baru): `DropPointSheet` tombol "Arahkan" → link Google Maps asli (target _blank), `RewardSheet` & `HelpTab` → state "Segera Hadir", `DashboardBanner` hapus tombol "Mulai Sekarang" dead, `ServiceGrid` hapus entry "Lainnya"
- [~] Migration `0002_add_pickup_verification_fields.sql` (kolom berat+foto) sudah dibuat, **belum dijalankan manual** (AutoMigrate sudah menambahkan kolom saat dev)

## Phase 3 — Redesign UI/UX (sesuai DESIGN.md)
- [~] Terapkan palet warna & tipografi baru — **token dasar selesai** (globals.css `@theme` + `:root`), komponen belum disentuh
  - [x] Design tokens di `globals.css`: `--color-primary` (emerald-700), status (pending/verifying amber-600, accepted blue-600, completed emerald-600, error red-600), skala `neutral-50..900`, `--radius-base` 12px, `--shadow-soft`; dark-mode block dihapus (Phase 2 opsional), Arial fallback di `body` dihapus (pakai Geist via layout)
- [x] Redesign stepper status pickup (Pending → Accepted → Verifying → Completed) — komponen `PickupStatusStepper` baru, dipasang di `ActivePickupCard` (pakai token CSS, pulse halus 2s, ikon centang lucide "Check")
- [ ] Redesign empty states (hapus elemen "sticker-like" kalau ada)
- [x] Redesign flow Mitra (radar list) — dense & functional
- [x] Redesign flow Warga (tenang & informatif)

## Phase 4 — PWA
- [x] Manifest lengkap + ikon multi-resolusi
- [x] Service worker (network-first untuk API)
- [ ] Uji Lighthouse PWA score ≥ 90, installable di Android/desktop

## Phase 5 — Cleanup Fitur Placeholder
- [x] Rapikan IPFS evidence flow (tetap dipertahankan, dirapikan tampilannya di riwayat)
- [x] Sembunyikan/hapus UI trigger untuk fitur out-of-scope (AI assistant, live map) atau beri label jelas "Coming soon" jika tetap ingin ditampilkan sebagai roadmap

## Phase 6 — Deployment & Portofolio Polish
- [x] Config production-ready (CORS/API URL/DSN dari env)
- [x] Fix lint errors + GetProfile repository refactor
- [x] Hapus dead code + migrasi token AuthModal/ProfileTab
- [ ] Deploy backend (Fly.io/Railway) + frontend (Vercel) + DB (Neon/Supabase)
- [ ] README final + demo GIF/video
- [ ] `.env.example` lengkap tanpa default unsafe

## Fase Polish Lanjutan (font, motion, modal center, notifikasi, help center)
- [x] Setup font Plus Jakarta Sans + framer-motion foundation — `layout.tsx` ganti Geist → `Plus_Jakarta_Sans` (weight 400/500/600/700 sesuai DESIGN.md §3), `globals.css` `--font-sans` diarahkan ke `--font-plus-jakarta-sans`, referensi `--font-geist-*` dihapus; `framer-motion` diinstall; konstanta bersama di `lib/motion.ts` (`tapScale`, `hoverScale`, `transitionFast`, `transitionModal`, `fadeSlideUp`) dibuat — belum diterapkan ke komponen (task terpisah)
- [x] Fix logout tertutup nav + ganti emoji greeting jadi ikon animasi — scroll container shared dipastikan `pb-32` (128px ≥ kebutuhan ±111px = tinggi BottomNav ±95px + gap 16px) sehingga tombol "Keluar Akun" di `ProfileTab` bersih dari `BottomNav` di semua tab; greeting di `app/page.tsx` ganti ikon `Hand` lucide warna `--color-primary` ukuran 20 sejajar nama, animasi wave rotate `[0,14,-8,14,0]` durasi 0.6s easeInOut pakai framer-motion, berjalan satu kali saat mount (keyframes di module scope biar tidak re-trigger saat re-render)
- [x] Modal/dialog terpusat (Dialog.tsx shared component) diterapkan ke semua sheet — buat `app/components/ui/Dialog.tsx` (backdrop `bg-black/40` + blur-sm, kartu tengah `flex items-center justify-center`, max-w-md, max-h 90vh + `overflow-y-auto`, animasi fade + scale 0.95→1 pakai `transitionModal` dari `lib/motion.ts`, tombol X pojok kanan, tutup via backdrop/X/Escape); konversi RewardSheet, DropPointSheet, PilahPintarSheet, AuthModal, modal detail di OrderHistory, dan 3 sub-modal ProfileTab (Edit Data/Alamat/Metode Penarikan) dari bottom-sheet/slide-up lama (fixed bottom, rounded-t-only, drag handle) ke Dialog terpusat — konten list/form tidak berubah, hanya wrapper positioning. Logic/state (useUIStore, activeModal, selectedOrder) tidak disentuh.
- [x] Hapus outer device-frame border, layout full-bleed responsive — frame "kotak HP" ditemukan di 2 halaman root (bukan `layout.tsx`): `app/page.tsx` & `app/collector/page.tsx` (pola `w-full max-w-md h-[100dvh] sm:h-[850px] ... sm:rounded-[2.5rem] shadow-2xl border border-neutral-100` + wrapper luar `flex items-center justify-center sm:p-8`). Diubah: mobile full-bleed (tanpa max-width/border), desktop `md:max-w-xl md:mx-auto` terpusat jelas tanpa frame — main kini `bg` transparan (halaman neutral-50 menyatu), height `h-[100dvh]` dipertahankan. BottomNav & elemen absolute tetap relatif ke viewport karena `main` tetap `relative h-[100dvh]`. Verifikasi CDP: mobile 390×844 main full-width 390px tanpa frame, nav gap 24px dari bottom; desktop 1366 main 576px terpusat (left 395/right 971), tinggi 900, nav gap 24px.
- [x] Pusat Notifikasi in-app (NotificationCenter + store) — 1) `store/useNotificationStore.ts`: Zustand TANPA persist (murni derived/sesaat, sengaja di-reset antar sesi), type `AppNotification {id,title,message,timestamp,read,type}` (type `'status_change'|'system'`), actions `addNotification/markAsRead/markAllAsRead/clearAll`, selector komputasi `selectUnreadCount`, cap 50 notif. 2) Sumber data DERIVED dari polling existing di `ActivePickupCard.tsx` (tidak ada polling baru; sisi kolektor dilewati karena `collector/page.tsx` tidak punya polling, hanya scan manual): `applyPickup` mendeteksi transisi status pickup yang sama via `prevActiveRef` → `addNotification` otomatis (PENDING→ACCEPTED "Pickup-mu diambil kolektor!", ACCEPTED→VERIFYING "Kolektor sudah menimbang"); transition COMPLETED ditambahkan di `handleConfirm` dengan nilai karma asli (`+X Karma`). Terverifikasi TYPE-only: transisi COMPLETED feedback nampil di notifikasi walau card unmount setelah polling refresh. 3) `app/components/dashboard/NotificationCenter.tsx` dibungkus `ui/Dialog.tsx` existing (bukan modal baru), list timestamp descending, tiap item title+message+waktu relatif (helper `timeAgo` lokal: Baru saja→menit→jam→hari→tanggal, tanpa library date), dot unread `--color-primary` + bg `primary/5`, tombol "Tandai semua terbaca" (muncul saat ada unread), klik item→`markAsRead`, empty state ikuti DESIGN §Empty states (`BellOff` bundar neutral-100 + teks singkat). 4) Trigger: entry point "Notifikasi & Radar" di `ProfileTab.tsx` disambungkan ke `useUIStore.openNotificationCenter`; TAMBAHAN icon bell + badge unread di header `app/page.tsx` sejajar icon profil (kanan atas), mount `<NotificationCenter/>` di root page. 5) Badge: dot merah `bg-status-error` absolute pojok icon bell, angka unread, `9+` jika >9. Reset notifikasi saat logout/logout-cross-account via `AuthBootstrap` (userData null → `clearAll`). State buka/tutup center ditambah ke `useUIStore` (`isNotificationCenterOpen`). `npm run build` sukses (TS clean) + `npm run lint` 0 error (satu warning pre-existing `setAddresses` di ProfileTab, di luar scope task).
- [x] Pusat Bantuan decision-tree (HelpCenter, ganti placeholder HelpTab) — 1) `lib/helpContent.ts`: data statis (interface `HelpAnswer`/`HelpCategory`, `helpCategories` 5 kategori: pickup/karma/akun/bukti/mitra) — konten PERSIS sesuai spec, field `icon` nama komponen Lucide. 2) `app/components/dashboard/HelpCenter.tsx`: komponen baru dibungkus `ui/Dialog.tsx` & `lib/motion.ts` (transitionModal), 3 level `category→question→answer` dengan state internal level/category/question, navigasi mundur per level (back `< Kategori`, `< [nama kategori]`, tombol "Kembali ke semua kategori" ke level 1 langsung). Transisi antar level pakai framer-motion `AnimatePresence mode="popLayout"` + variants slide horizontal berarah (`slideVariants` custom = moveDir): masuk dari kanan saat drill-down, keluar ke kanan saat back — bukan ganti konten instan. Icon kategori via `ICON_MAP` string→component (fallback `HelpCircle`). Search box level 1 DIKERJAKAN: filter kategori + pertanyaan by text, ada tombol clear & empty state. Reset state penuh saat dialog ditutup (handleClose). Anti-slop: ikon Lucide solid, netral, tanpa "Pilah AI"/Sparkles (HelpTab placeholder & merek AI dibuang). 3) Trigger: `help tab` BottomNav ke-3 di `page.tsx` di-intercept (`onTabChange`) → `openHelpCenter()` (tidak lagi switch tab), import HelpTab dihapus & render branch `help` dihapus; entry "Pusat Bantuan" di `ProfileTab.tsx` → `openHelpCenter()`. `HelpTab.tsx` (placeholder "Segera Hadir") DIHAPUS. `useUIStore` ditambah `isHelpCenterOpen/open/close`. `npm run build` sukses (TS clean) + `npm run lint` 0 error.
- [x] Auto-cancel pickup PENDING setelah 3 menit + retry — BACKEND (`backend`): endpoint polling `GET /api/v1/pickups/history` → `handlers.GetUserHistory` → `services.pickupService.GetUserHistory`. Konstanta `PickupExpiryDuration = 3 * time.Minute` didefinisikan sekali di `services/pickup_service.go` (bukan magic number). Lazy auto-cancel: sebelum history dikembalikan, `cancelExpiredPendingPickups(userID)` membatalkan semua pickup user berstatus PENDING yang `created_at < now - expiry`, via `repo.CancelExpiredPendingPickups` (bulk UPDATE `WHERE user_id=? AND status='PENDING' AND created_at<?` dalam 1 transaction sederhana — WHERE melindungi race dengan AcceptPickup). Status `CANCELLED` aman: model cuma string `default:'PENDING'` tanpa CHECK constraint DB, dan semua path proses (Accept/FindByID*) sudah WHERE status spesifik sehingga pick-up ter-cancel tidak bisa di-accept/diproses. FRONTEND (`frontend`): `ActivePickupCard.tsx` tangani `CANCELLED` sebagai state terminal — saat pickup aktif hilang & record terbaru CANCELLED, stepper tidak dirender; diganti card `XCircle` warna `--color-status-error` + pesan informal "Yah, orderan kamu dibatalkan otomatis... Belum ada kolektor yang gercep... dibatalkan setelah 3 menit..." + tombol "Coba Lagi" yang memicu flow create existing (`onRetry={handleJemput}` dari `app/page.tsx`, reuse logic, bukan bikin baru) — card lama hilang, flow baru seperti biasa; polling di-hentikan saat card CANCELLED tampil (terminal, seperti COMPLETED) dan dilanjutkan lagi setelah retry. `OrderHistory.tsx`: badge CANCELLED ditambahkan (ikon XCircle + teks "Jemputan Dibatalkan" + warna `--color-status-error` di list & modal detail; query GetUserHistory memang sudah mengembalikan semua status termasuk CANCELLED, tidak perlu ubah query). `go build` OK, `go vet` OK, `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`). Testing 3 menit bisa dipercepat: perkecil `PickupExpiryDuration` sementara (mis. `5 * time.Second`), test, kembalikan ke `3 * time.Minute`.
- [x] Pusat Bantuan jadi floating icon (HelpFAB), BottomNav 3 tab — entry point Pusat Bantuan dipindah dari tab BottomNav ke FAB. `BottomNav.tsx`: tab "help/Bantuan" dihapus → 3 tab (Beranda/Pesanan/Profil), layout `flex justify-between` di pill glassmorphism tetap center & proporsional secara otomatis. `app/components/dashboard/HelpFAB.tsx` (baru): tombol bulat `w-14 h-14` (56px), `fixed` kanan bawah `right-4` (16px), posisi bottom `calc(var(--bottom-nav-height, 128px) + 20px)` memakai CSS variable ukuran BottomNav existing → di ATAS nav, tidak menutupi; ikon `MessageCircleQuestion` (lucide) putih di atas `--color-primary`, `shadow-soft`, animasi `whileHover={hoverScale}` & `whileTap={tapScale}` dari `lib/motion.ts` + `transitionFast`; `z-40` (di atas konten z-10 & nav z-30, di bawah Dialog z-[70]); onClick → `openHelpCenter()` dari `useUIStore` (state yang sudah ada, bukan baru), buka `HelpCenter` yang sudah ada tanpa mengubah logic decision-tree-nya. Mount hanya di sisi warga (`app/page.tsx`); intercept tab "help" lama di `page.tsx` dihapus total (`onTabChange={setActiveTab}`), `openHelpCenter` tidak lagi di-destructure di page. Entry "Pusat Bantuan" di `ProfileTab.tsx` TETAP ada sebagai akses alternatif. `npm run build` sukses (TS clean) + `npm run lint` 0 error (1 warning pre-existing `setAddresses`).
- [x] Fix akun kolektor test (id 20) + tambah pilihan role saat register — **AKAR MASALAH "Akses ditolak untuk role ini"**: BUKAN bug middleware. Semua akun di DB ber-role `'user'` (default model `models/user.go`), tidak ada satupun `'collector'`, dan tidak ada jalur apa pun untuk membuat akun kolektor — `RegisterRequest` (`handlers/auth.go`) hanya `name/email/password`, handler tidak menyetel role, frontend `registerUser` & `AuthModal.tsx` juga tidak mengirim role. Jadi JWT (dibangun dari `user.Role` di `auth_services.go`) selalu berisi `role:"user"`, dan `RequireRole("collector")` menolak. Middleware `Protected()`/`RequireRole` + type assertion `.(string)` sudah benar. **Task 1 (data)**: `UPDATE users SET role='collector' WHERE id=20` (rahel/ghifariakbar2006@gmail.com) → kolom `'collector'` persis (role_len 9). Ghif wajib logout-login ulang di browser supaya token baru membawa claim `role:"collector"`. **Task 2 (akar masalah, kode)**: BACKEND `handlers/auth.go` — `RegisterRequest` + field `role`; `RegisterUser` validasi whitelist via helper `normalizeRole`: kosong→default `'user'`, selain `"user"`/`"collector"`→400 Bad Request (tidak percaya input client). FRONTEND `lib/api.ts` — `registerUser(name,email,password,role)` kirim `role`; `AuthModal.tsx` — form register mode non-login tambah toggle 2 tombol "Daftar sebagai Warga"/"Mitra" (`background/ring` pakai `--color-primary`, ikon `UserIcon`/`Truck` lucide, tanpa ilustrasi/stiker) + teks konteks singkat per pilihan, state `role` di-submit ke backend. Tidak ada logic auto-redirect berbasis role di app (kedua halaman statis) → flow register tetap di halaman yang sama, user Mitra bisa lanjut manual ke /collector. Verifikasi endpoint: role=`collector`→201 & role tersimpan, role=`admin`→400 whitelist, tanpa role→201 default `user` (test user dibersihkan setelahnya). `go build` OK, `go vet` OK, `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`).
- [x] Fix state management ActivePickupCard (completed & cancelled tidak clear/tumpang tindih) — **AKAR MASALAH**: (1) 2 state terpisah (`pickup` utk aktif + `cancelledPickup` utk CANCELLED) & TIDAK ada guard respon stale — polling dipicu dari banyak sumber yang overlap (effect mount/identitas + `setInterval` 5s + `await pollActive()` eksplisit setelah confirm), respon `getUserHistory` yang men-snapshot status LAMA bisa resolve SETELAH perubahan dan menimpa state baru (`setPickup(staleActive)` + `setCancelledPickup(null)`) → card lama di-resurrect: Gejala A = respon stale VERIFYING datang setelah confirm → card "Mencari Kolektor/Menunggu" muncul lagi di atas banner sukses "Selesai! +X Karma" (`feedback` TIDAK pernah di-clear di `applyPoll`); Gejala B = respon stale PENDING datang setelah auto-cancel → menimpa card batal, card lama tampil lagi. (2) TIDAK ADA branch terminal COMPLETED (cuma `setPickup(null)`) + tidak ada mekanisme dismiss. **FIX** (`ActivePickupCard.tsx`): SATU source of truth `pickup` (status apa pun) + replace TOTAL di `applyPoll` (bukan merge parsial); guard `pollSeqRef` (tiap poll mulai ++counter, respon dengan seq ≠ latest → DIBUANG) cegah stale overwrite; `terminalRef` menghentikan polling saat card terminal tampil; `dismissedRef` mencegah COMPLETED yang sudah user tutup di-resurrect polling; `feedback` di-clear saat pickup berganti/`null`; render = 1 switch jelas: PENDING/ACCEPTED/VERIFYING→stepper, COMPLETED→card sukses (stepper "Selesai" semua check + ShieldCheck + banner karma + tombol **Tutup** yang clear `pickup`→balik ke ServiceGrid), CANCELLED→card batal + Coba Lagi. Pilih dismiss MANUAL (bukan auto-dismiss): biar user sempat membaca jumlah karma sekaligus konsisten dengan card batal yang butuh aksi eksplisit (Coba Lagi). Verifikasi E2E (API+CDP 390×844, backend dengan konstanta dipercepat 5s utk CANCELLED lalu dikembalikan 3m): **COMPLETED** flow create→accept→complete→confirm → card sukses tampil (stepper "Selesai", tanpa "Mencari Kolektor", ada banner `+50 Karma` saat confirm dari card, tombol Tutup), klik Tutup → card hilang & kembali ke ServiceGrid; **CANCELLED** flow create→expire → card batal + Coba Lagi tampil BENAR (bukan card lama), stabil setelah +6s (tidak flapping balik — guard stale bekerja). `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`).

- [x] Fix WaitingRadar muncul lagi setelah dismiss card Selesai — **AKAR MASALAH (PERSIS)**: `WaitingRadar` ("Mencari Pahlawan...") dirender berdasarkan `orderStatus.type === 'success'` (`app/page.tsx`), dan **`orderStatus` hanya bisa balik ke `'idle'` lewat tombol "Batalkan Pencarian"** (`handleCancelRadar`). `isSearching` (`useUIStore`) TIDAK pernah terhubung ke WaitingRadar — hanya untuk disable+spinner tombol "Jemput" di `ServiceGrid` (dan `startSearching`/`stopSearching` SEBENARNYA sudah selalu balance: `stopSearching()` selalu dipanggil di `finally`/path error `handleJemput`). Jadi flag yang NYANGKUT bukan `isSearching` melainkan **`orderStatus`**: setelah `createPickupRequest` sukses (page:55) ia tetap `'success'` selamanya — tidak di-reset saat pickup PENDING mulai tampil, saat confirm COMPLETED, maupun saat tombol **Tutup** (dari fix task sebelumnya) yang cuma `setPickup(null)` di ActivePickupCard. Akibatnya begitu card COMPLETED di-hide, WaitingRadar tampil lagi. **FIX** (`app/page.tsx` + `ActivePickupCard.tsx`): (1) `ActivePickupCard` ekspose callback `onPickupVisibleChange(visible)` yang dikirim lewat `useEffect([pickup])` — page memakai ini sebagai PENDORONG SATU source-of-truth render: begitu card pickup APA PUN (aktif/terminal) mulai tampil, `handlePickupVisibleChange` mereset `orderStatus→'idle'` sekaligus `stopSearching()` (lewat `useUIStore.getState()`). Konsekuensinya WaitingRadar HANYA tampil saat "request terkirim tapi belum ada pickup tampil" — otomatis menghilang begitu card PENDING muncul, dan TIDAK MUNGKIN muncul lagi setelah Tutup (orderStatus sudah idle). (2) tombol **Tutup** (COMPLETED) & **Coba Lagi** (CANCELLED) juga memanggil `useUIStore.getState().stopSearching()` sebagai bagian clear state total. (3) `page.tsx` tambah state `hasActivePickup` (di-set dari callback yang sama) supaya `ServiceGrid` tetap disabled selama pickup aktif (cegah double-submit), tidak hanya saat radar. `npm run build` sukses (TS clean) + `npm run lint` 0 error (1 warning pre-existing `setAddresses`). Verifikasi E2E manual full flow (API+CDP 390×844, register warga baru + kolektor `mitra@pilah.test`, geolocation emulasi CDP `Emulation.setGeolocationOverride`): klik Jemput → WaitingRadar tampil (true) → kolektor accept+complete → card VERIFYING tampil di UI (true) → confirm → card COMPLETED + tombol Tutup (true) → klik Tutup → 3s kemudian `completedGone=true, serviceGridBack=true, radarNow=false, radarCancelBtn=false` → **watch 10 detik tiap 500ms: radar tidak pernah muncul lagi (false/NO)** → final: `radar=false, gridBack=true`.

- [x] Formula karma berbasis berat (60/24/6 per kg) — ganti flat `+50 Karma` jadi perhitungan dinamis di `backend/internal/services/pickup_service.go`. Konstanta jelas di atas file: `KarmaPerKgPlastik=60`, `KarmaPerKgKardus=24`, `KarmaPerKgKaca=6` (sesuai PRD: "Perhitungan poin berbasis berat per material"). Helper `calculateKarma(p models.Pickup) int` = `math.Round(plastic*60 + cardboard*24 + glass*6)` (import `math`), menggantikan `const karmaGained = 50` di `ConfirmPickup` — konstanta Go tidak bisa memanggil fungsi, jadi dihitung setelah pickup di-load dalam transaksi (`karmaGained := calculateKarma(*pickup)`). **Pilihan fallback (jelas)**: (a) total berat > 0 tapi hasil pembulatan < 1 (mis. kaca 0.04 kg → 0.24 → round 0) → floor `minKarmaForValidPickup=1` supaya transaksi valid tidak memberikan 0 karma; (b) semua berat 0 (data lama/kosong) → `fallbackKarmaOldData=5` sebagai penghargaan dasar. Tanpa ubah skema DB (murni logic kalkulasi). IPFS evidence yang sempat hardcode `"+50 Karma"` ikut dibenahi: `pinPickupToIPFS(repo, pickup, karma)` memakai nilai aktual `"+<karma> Karma"` (import `strconv`). Frontend DIJAMIN tanpa perlu diubah: `confirmPickup()` (`lib/api.ts`) sudah mengambil `karma` dinamis dari response `{ pickup_id, karma }`, dan `ActivePickupCard.tsx` menampilkan `+${res.karma} Karma`. `go build` OK + `go vet` OK. Verifikasi API (register warga baru + kolektor `mitra@pilah.test`): input plastik 2 kg + kardus 1 kg + kaca 0.5 kg → **response karma 147** = 2×60 + 1×24 + 0.5×6 ✓ (karma_points user 0 → 147, delta cocok). Edge case: semua berat 0 → **5** ✓; hanya kaca 0.04 kg → **1** ✓. Verifikasi UI (CDP 390×844, klik tombol "Konfirmasi Selesai" langsung di browser): feedback tampil **"+147 Karma"** + card COMPLETED ✓, tanpa teks "+50" stale.
- [x] Estimasi berat warga (EstimateWeightDialog) + tampil di radar kolektor — fitur opsional: warga bisa input estimasi berat (plastik/kardus/kaca kg) di step terpisah SETELAH klik "Jemput Sampah" dan SEBELUM request GPS dikirim. Estimasi HANYA referensi untuk kolektor di radar — **bukan sumber karma final** (karma tetap dihitung dari berat aktual yang disubmit kolektor di `ConfirmPickup`/`calculateKarma`, task sebelumnya). **BACKEND**: (1) `models/pickup.go` tambah 3 kolom `EstPlasticWeight/EstCardboardWeight/EstGlassWeight` (float64, `gorm:"default:0"`, JSON tag `est_*_weight`) — ditempatkan sebelum `PlasticWeight` aktual supaya jelas dipisahkan sebagai estimasi; (2) migration baru `backend/migrations/0003_add_pickup_estimation_fields.sql` (3 `ADD COLUMN IF NOT EXISTS ... double precision NOT NULL DEFAULT 0`) — tidak auto-run, AutoMigrate (`config/database.go:52`) handle dev seperti pola sebelumnya; (3) handler `CreatePickup` (`handlers/pickup.go`) terima 3 field opsional dari body (default 0 kalau tidak dikirim) teruskan ke service; service interface + `CreatePickup(userID, lat, lng, estPlastic, estCardboard, estGlass)` simpan ke record baru; (4) `GetPendingPickups` (radar) otomatis mengembalikan 3 field via JSON tag (diverifikasi). **FRONTEND**: (5) `components/dashboard/EstimateWeightDialog.tsx` (baru) — dibungkus `ui/Dialog.tsx` existing: 3 input angka (Plastik/Kardus/Kaca, opsional, min 0, step 0.1, format `kg` suffix, spinner number dihapus via inputmode decimal), teks penjelasan informal "Estimasi aja, gak perlu presisi — kolektor bakal timbang ulang pas di lokasi." (ikon `Scale`), tombol **"Lanjut"** → `onContinue(WeightEstimate)` dengan nilai (kosong→0), tombol **"Lewati"** → `onSkip` lanjut semua 0 (TIDAK memaksa isi); input di-reset tiap dialog dibuka/tutup; (6) `app/page.tsx` — `handleJemput` kini dua fase: klik Jemput → `setIsEstimateOpen(true)` (BUKAN langsung GPS); `proceedWithEstimate(estimate)` simpan ke `pendingEstimateRef` lalu `sendPickupRequest()` = flow GPS+create existing, kirim juga estimasi (`createPickupRequest(lat, lng, estimate)`); `skipEstimate()` = lanjut 0; menu <`EstimateWeightDialog/>` di-mount di root page; (7) `lib/api.ts` — `Pickup` interface + `est_*_weight`; `createPickupRequest(lat, lng, estimate?)` kirim 3 field (hanya kalau `!== undefined`); (8) `collector/page.tsx` — helper `buildEstimateChips(pickup)` menyusun chip `~2kg Plastik`/`~1kg Kardus`/`~0.5kg Kaca` HANYA untuk material > 0 (kalau semua 0 → array kosong, TIDAK ada placeholder — prinsip empty state DESIGN.md), dirender sebagai badge rounded-full kecil di item radar. `go build` OK + `go vet` OK + `npm run build` sukses (TS clean) + `npm run lint` 0 error (1 warning pre-existing `setAddresses`). Verifikasi API: CASE A (estimasi 2/1/0.5 terkirim) → radar `est: 2/1/0.5` ✓; CASE B (skip, field tidak dikirim) → radar `0/0/0` ✓; CASE C (0 eksplisit) → `0/0/0` ✓; `has est keys in response: true`. Verifikasi UI (CDP 390×844): klik Jemput → dialog "Estimasi Berat Sampah" terbuka + teks penjelasan ada ✓; isi plastik 2 + kardus 1 → klik Lanjut → GPS (emulasi) → WaitingRadar muncul (create sukses) ✓; sisi kolektor klik Scan → badge radar **"~2kg Plastik"** tampil ✓.

- [x] Profil Mitra tersimpan ke backend & bisa diedit (ganti data hardcoded + hapus rating fake) — **TEMUAN**: (1) tab Profil kolektor (`collector/page.tsx`) menampilkan data **HARDCODED** "Motor Bak / Jakarta Pusat / BCA ****1234" + rating **4.9** palsu; (2) "Edit Data Diri" warga di `ProfileTab.tsx` cuma `setUserData` (state Zustand lokal, tidak pernah ke backend). **BACKEND**: `models/user.go` tambah `VehicleType/ServiceArea/BankName/BankAccountNumber` (string `gorm:"size:50/100;default:''"`, JSON `vehicle_type/service_area/bank_name/bank_account_number`) agar warga (role user) tidak terpengaruh; migration `0004_add_user_profile_fields.sql` (4 `ADD COLUMN IF NOT EXISTS ... varchar NOT NULL DEFAULT ''`; dev ditangani AutoMigrate `config/database.go:52`); `user_repo.go` — `GetByID` Select diperluas dari `id/name/email/karma_points/role` menjadi +4 kolom profil (password tetap tidak pernah di-select) + method baru `UpdateProfile(id, updates map[string]interface{})` (GORM `Updates` dengan map = partial update); `auth_services.go` — interface + `UpdateProfile(userID, ProfileUpdate) (*models.User, error)`: bangun map hanya dari field non-kosong (`Name/VehicleType/ServiceArea/BankName/BankAccountNumber`), kalau kosong semua → error (tidak ada partial update yang menimpa data), lalu `repo.UpdateProfile` + return `GetByID` terbaru; `handlers/auth.go` — `UpdateProfileRequest` (5 field), handler `UpdateProfile`: user_id dari `c.Locals("user_id")` (JWT, BUKAN dari body — cegah edit akun orang lain), `strings.TrimSpace` tiap field, error → 400, sukses → `{status,data:user}`; `main.go` — route baru `api.Patch("/users/me", middleware.Protected(), handlers.UpdateProfile)` (CORS AllowMethods sudah include PATCH, tidak ada perubahan CORS). **FRONTEND**: `lib/api.ts` — `UserProfile` + 4 field opsional, `ProfileUpdatePayload` + `updateProfile(payload)` (PATCH `/users/me`, `credentials:include`); `store/useAuthStore.ts` — `UserData` + 4 field opsional; `ProfileTab.tsx` — `saveProfile` kini async → `updateProfile({name})`, `setUserData` dari response, error → toast "Gagal menyimpan"; `collector/page.tsx` — tab Profil: baris kendaraan/area/rekening dari `userData` (empty → jujur "Belum diatur", bukan dummy), rating "4.9" DIHAPUS → badge `<CheckCircle2/> {completedMissions} Pickup Selesai` (dari `getCollectorHistory()`, effect history diperluas ke tab profile supaya badge terhitung tanpa buka Riwayat), tombol **"Edit Profil Mitra"** (ikon `Pencil`, warna primary) → dialog `ui/Dialog.tsx`: 4 input (Jenis Kendaraan/Area Operasional + Nama Bank/No. Rekening grid 2 kolom) dengan placeholder benar, tombol "Simpan Perubahan" → `saveProfileMitra`: `updateProfile` → `setUserData` (perpaduan field profil) → tutup dialog + banner sukses; error via banner. **Verifikasi**: `go build ./...` OK, `go vet ./...` OK, `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`). API (`test_profile_api.mjs`, mitra `mitra@pilah.test`) — login → set semua 4 field PATCH → persisted di GET berikutnya; PATCH hanya `name` → field lain TIDAK berubah (partial update benar); body kosong/whitespace → 400. UI CDP 390×844 (`verify_profil_ui.mjs` + debug_profil.mjs): dialog Edit Profil Mitra terbuka, ubah kendaraan → banner "Profil mitra diperbarui!" + baris ke-update live + setelah `Page.reload` nilai tetap tampil; DB dikonfirmasi langsung via API (vehicle_type="Motor Bak"). Warga (`verify_profil_warga.mjs`): Edit Data Diri terbuka dgn nama default dari userData, simpan → toast "Profil berhasil diperbarui!" + header nama update live + reload persists (endpoint sama, nama-only PATCH terverifikasi di API test). Catatan: script test awal sempat salah baca karena innerText di-uppercase CSS untuk badge/label (`7 PICKUP SELESAI`, `NO. REKENING`) dan envelope API dua-level (`data.data`), bukan bug aplikasi.

- [x] Fix card Selesai muncul lagi setelah pindah tab (dismissedRef → store global) — **AKAR MASALAH (PERSIS seperti dugaan)**: (1) `renderContent()` di `app/page.tsx` hanya merender `<ActivePickupCard/>` di branch default/home (baris 116-140) — saat `activeTab === 'orders' || 'profile'` komponen **unmount total**; (2) `dismissedRef` di `ActivePickupCard.tsx` adalah `useRef` LOKAL (baris 76, `useRef<string | null>(null)`) — nilainya HILANG tiap komponen unmount/remount. Akibatnya: user tutup card COMPLETED → `dismissedRef` terisi id → pindah tab (ref & polling mati, interval di-clear) → balik Home → komponen remount, `dismissedRef` kembali `null`, polling jalan lagi, history mengembalikan pickup COMPLETED terbaru → `applyPoll` tidak punya "ingatan" dismissal → card Selesai tampil lagi. **FIX**: (3) `store/useUIStore.ts` tambah state global `dismissedPickupId: string | null` + `setDismissedPickupId(id)` (TIDAK dipersist ke localStorage — store ini memang tanpa persist, reset saat refresh wajar; cukup bertahan sepanjang sesi browser). (4) `ActivePickupCard.tsx` — `dismissedRef` DIHAPUS; semua pemakaian diganti read/write `useUIStore.getState()`: `handleDismissCompleted` → `setDismissedPickupId(pickup.id)`, branch `if (active)` → `setDismissedPickupId(null)` (pickup baru muncul = dismissal lama tidak relevan), branch `if (terminal)` → `if (terminal.status === 'COMPLETED' && useUIStore.getState().dismissedPickupId === terminal.id)` skip. Baca ditulis imperatif lewat `getState()` (bukan hook) di dalam `applyPoll` (useCallback dep `[detectStatusChange]`) supaya tidak ada stale closure — pola konsisten dengan `stopSearching()` yang sudah dipakai. (5) Logic lain TIDAK berubah (COMPLETED yang ditutup + polling dilarang resurrect, sama seperti sebelum). (6) **Ref lain yang DIBIARKAN lokal + alasan**: `terminalRef` — reset saat remount memang diinginkan (polling HARUS lanjut lagi setelah tab balik; berkat `dismissedPickupId` di store, polling tsb tidak me-resurrect card Selesai). `retryingRef` — kalau ter-reset "lebih awal" (pindah tab di tengah flow Coba Lagi) cuma akan menampilkan kembali card CANCELLED yang memang status sebenarnya; begitu pickup baru muncul, `applyPoll` membersihkannya. `pollSeqRef` — respon stale tidak bisa lintas instance (closure lama di-destroy saat unmount, interval di-clear), seq di-reset lalu dihitung ulang dari 0 tanpa konflik. `prevActiveRef` — murni dedup notifikasi; reset cuma berpotensi melewatkan/duplikasi SATU notifikasi, tidak me-resurrect card. **Verifikasi**: `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`). Test manual E2E (CDP 390×844, `verify_dismiss_tab.mjs`): register warga baru → create pickup via API → accept/complete/confirm (semua 200) → card COMPLETED tampil → klik **Tutup** → card hilang (watch 6.5s) → pindah tab **Profil** → balik **Beranda** (watch 13s = ≥2 cycle polling setelah remount) → card TIDAK muncul lagi ✓ → ulangi tab **Pesanan** → card TIDAK muncul lagi ✓. Kontrol positif (`verify_dismiss_control.mjs`, metodologi valid): pickup COMPLETED kedua TANPA dismiss → pindah tab → balik → card MEMANG muncul lagi (membuktikan mekanisme resurrect nyata & polling remount tetap hidup) — suppression murni karena `dismissedPickupId` di store, bukan polling yang rusak.
- [x] Framer-motion micro-interaction di sisi kolektor (konsisten dengan sisi warga) — `frontend/app/collector/page.tsx` (SATU-SATUNYA file yang diubah, logic/state TIDAK disentuh) tetap pakai konstanta existing di `lib/motion.ts` (`tapScale 0.97`, `hoverScale 1.02`, `transitionFast 0.15s`, `fadeSlideUp`, `transitionModal`), tambah import `AnimatePresence, motion` di baris atas. Elemen yang beranimasi: (1) **Pin peta** (marker titik sampah, kondisi `!activeOrder`) → dibungkus `AnimatePresence`, tiap pin jadi `motion.button` dengan **fade-in/out OPACITY-ONLY** (`initial/animate/exit opacity`, `transitionFast`) — sengaja TIDAK pakai transform/scale/hover di pin karena framer inline transform akan menimpa class Tailwind `-translate-x-1/2 -translate-y-full` yang mengatur posisi (efek "hover/sorot" pin tetap via CSS `hover:scale-105`/`scale-110` di class, tanpa konflik); (2) **List titik jemput (radar)** → tiap item jadi `motion.div` dengan `{...fadeSlideUp}` (masuk: opacity 0→1 + y 8→0) + `exit={{opacity:0, y:-8, transition:transitionFast}}` + `whileHover={hoverScale}` → muncul & hilang mulus saat scan menyala/order di-accept; (3) **List riwayat perjalanan (tab Riwayat)** → item jadi `motion.div` sama (fadeSlideUp + exit + whileHover); (4) **Tombol CTA** → jadi `motion.button` `whileTap={tapScale}` + `transition={transitionFast}`, class CSS `active:scale-[0.98]` DIHAPUS (hindari double-transform, framer inline menang): "Konfirmasi Selesai", "Ambil Tugas Ini", "Scan Area", "Edit Profil Mitra", "Akhiri Shift", "Simpan Perubahan" (dialog profil), "Masuk Sekarang" (blok "Belum masuk sebagai mitra"); (5) **Dialog "Edit Profil Mitra"** — TIDAK diubah kode-nya karena sudah memakai `app/components/ui/Dialog.tsx` (baris import 10) yang sejak task dialog-terpusat sudah beranimasi `AnimatePresence` + fade + scale 0.95→1 `transitionModal` (verified ulang `Dialog.tsx` berisi motion). `npm run build` sukses (TS clean) + `npm run lint` 0 error (1 warning pre-existing `setAddresses` di ProfileTab, bukan dari task ini). Tidak ada penambahan konstanta motion baru, tidak ada perubahan logic polling/scan/accept/complete/logout.

- [x] Tukar Cuan fungsional (redeem karma real backend + simulasi payout) — **BACKEND**: endpoint baru `POST /api/v1/karma/redeem` (middleware `Protected()`, di `main.go` setelah route `/users/me`). `handlers/auth.go` — `RedeemKarmaRequest{amount_karma int}` + handler `RedeemKarma`: user_id dari JWT `c.Locals("user_id")` (bukan body), request <= 0 → 400, sukses → `{status, data:{new_balance, rupiah_value: amount* services.KarmaToRupiahRate}}`. `services/auth_services.go` — konstanta `KarmaToRupiahRate = 50` (kurs simulasi 1 karma = Rp50, satu-satunya sumber rate) + interface & impl `RedeemKarma(userID, amountKarma) (newBalance int, err error)`: validasi `amountKarma <= 0` → error, lalu transaksi nyata: `repo.Begin()` → `repo.GetUserByIDForUpdate(tx, userID)` (**row lock FOR UPDATE** di repo, mencegah race dua redeem) → kalau `user.KarmaPoints < amountKarma` → rollback + error tepat **"Karma kamu gak cukup buat jumlah segini"** → `repo.DeductKarma(tx, userID, amountKarma)` (GORM `karma_points - ?`) → commit. `repository/user_repo.go` — interface + `Begin()`, `GetUserByIDForUpdate(tx, ...)` (Clauses Locking UPDATE), `DeductKarma(tx, ...)`. **FRONTEND**: `store/usePaymentMethodStore.ts` (BARU) — `paymentMethods/selectedMethodId/addPaymentMethod/setSelectedMethodId`, **di-persist ke localStorage** (`pilah-payment-methods`) karena hanya preferensi UI (bukan data finansial asli), default GoPay + Bank Transfer. `lib/api.ts` — `RedeemKarmaResponse{new_balance, rupiah_value}` + `redeemKarma(amountKarma)` (POST `/karma/redeem`). `ProfileTab.tsx` — state `paymentMethods` lokal DIHAPUS, refactor pakai `usePaymentMethodStore` (tambahan dari sini otomatis dibaca RewardSheet; tidak ada perubahan visual/flow modal). `components/dashboard/RewardSheet.tsx` — GANTI TOTAL placeholder "Segera Hadir": (1) saldo kartu gelap (karma aktif + "Senilai Rp X" pakai `toLocaleString('id-ID')` + keterangan "Kurs 1 Karma = Rp50"); (2) **pilihan jumlah = tombol "Tarik Semua Karma"** (pendekatan FULL BALANCE, bukan input angka — lebih simpel & tanpa edge-case validasi; card selectable selalu terpilih, menampilkan `X Karma = Rp Y`); (3) radio metode payout dari store (ikon Smartphone/CreditCard sesuai kind, selected → border-primary); (4) tombol **"Tarik Sekarang"** `motion.button` `whileTap` + `disabled={karma<=0 || !selectedMethod}` + hint kecil "Karma masih 0..." saat kosong; (5) flow: klik → `redeemKarma(karma)` REAL dulu (backend kurangi saldo) → `setTimeout` 1600ms (delay artifisial) → `getUserProfile()` + `setUserData()` (refresh userData → KarmaWallet ikut 0) → layar sukses `ShieldCheck` hijau `--color-status-completed`, **"Dana Rp X telah dikirim ke GoPay"** + disclaimer kecil WAJIB `text-xs text-neutral-400`: "Simulasi untuk keperluan portofolio — dana tidak benar-benar terkirim." + tombol Selesai; (6) phase machine `idle|processing|success` dibungkus `AnimatePresence mode="wait"` + `motion.div` (processing: `Loader2 animate-spin` + "Memproses penarikan..."), reset ke idle via `handleClose` (bukan useEffect — lolos lint `react-hooks/set-state-in-effect`); (7) Dialog.tsx + konstanta `lib/motion.ts` DIPAKAI, tidak ada animasi/modal baru dari nol. Verifikasi manual (API + CDP 390×844, user fresh diset karma 147 via DB): **DB sebelum = 147** → redeem amount 999 → 400 "Karma kamu gak cukup buat jumlah segini" & DB TETAP 147 → UI buka Tukar Cuan tampil "Saldo Karma Aktif 147 / Senilai Rp 7.350" + "Tarik Semua Karma 147 Karma = Rp 7.350" → Tarik Sekarang → layar "Memproses penarikan..." (spinner, ~1.6s) → "Penarikan Berhasil! Dana Rp 7.350 telah dikirim ke GoPay" + disclaimer tampil → **DB sesudah = 0** (REAL berkurang, bukan cuma UI) → refresh browser → Saldo Karma 0 + "Tidak ada karma untuk ditarik" + `GET /users/me` karma_points 0 (bukti dari backend bukan cache). User test dibersihkan. `go build` OK + `go vet` OK, `npm run build` sukses (TS clean) + `npm run lint` 0 error (1 warning pre-existing `setAddresses` di ProfileTab).

- [x] Perbaiki tombol "Keluar Akun" di ProfileTab yang MASIH tertutup BottomNav (walaupun pb-32 sempat dipasang)
  - Akar masalah sebenarnya: root `ProfileTab` memakai `h-full` -> box terkunci setinggi viewport, konten logout overflow turun ke area nav sehingga padding clearance di container scroll tak efektif (di sinilah estimasi manual meleset menyesatkan). `h-full` dihapus.
  - Solusi dinamis: `BottomNav` mengukur tinggi aslinya via ref + getBoundingClientRect (fallback ResizeObserver/resize/orientationchange saat 100dvh berubah) lalu ekspos CSS var `--bottom-nav-height` di root. Wadah scroll pakai `padding-bottom: calc(var(--bottom-nav-height, 128px) + 16px)`.
  - Greeting dashboard jadi teks polos "Halo, {nama}!" (ikon Hand + animasi wave dihapus, sesuai DESIGN.md anti-AI-slop).
  - Verifikasi TERUKUR (bukan kalkulasi): headless Edge 390x844 + login sungguhan -> tab Profil -> scroll maksimum: logout bottom=305, nav top=354, clearance=49px, `visibleOk=true`. Screenshot di `%TEMP%\opencode\profil_verify.png`.
  - `npm run build` production: sukses, TS clean.

- [x] Update konten FAQ entry `k2` (kategori karma) di `frontend/lib/helpContent.ts` — hapus teks basi "Segera Hadir" (Tukar Karma placeholder sudah diganti Tukar Cuan fungsional, task sebelumnya), ganti dengan jawaban baru persis: "Karma kamu bisa langsung ditukar jadi saldo lewat menu Tukar Cuan di halaman utama — tinggal pilih metode pembayaran (GoPay, DANA, bank, atau apa pun yang kamu tambahkan sendiri), lalu tarik semua karma sekaligus. 1 karma setara Rp50." Entry FAQ lain tidak diubah. `npm run build` sukses (TS clean).

- [x] Fix form input berat kolektor (root cause breakdown 0kg) — **ROOT CAUSE PERSIS**: `handleComplete` di `collector/page.tsx` memanggil `completePickup(activeOrder.id.toString())` TANPA body — tidak pernah ada form/input berat di sisi kolektor. Backend `CompletePickup` (handler `pickup.go` + service `pickup_service.go`) memang SUDAH menerima body `PickupVerification` (plastic/cardboard/glass/photo), tapi karena body kosong (`len(c.Body())==0`) semua berat tersimpan 0 → breakdown warga selalu 0kg saat VERIFYING & karma pakai fallback. **FIX**: buat `frontend/app/components/dashboard/CollectorCompleteDialog.tsx` (baru) — Dialog.tsx existing + motion, konsisten dgn `EstimateWeightDialog` tapi WAJIB isi: 3 input angka (Plastik/Kardus/Kaca, step 0.1, min 0), validasi total berat > 0 dengan pesan "Isi minimal 1 jenis berat sebelum menyelesaikan", tombol "Kirim & Selesaikan" memanggil `completePickup(id, {plastic_weight, cardboard_weight, glass_weight})` dengan nilai aktual. `collector/page.tsx` — tombol "Konfirmasi Selesai" kini buka dialog (bukan langsung panggil API), `handleComplete` (langsung API tanpa data) dihapus, `submitVerification` baru kirim data berat, state `isCompleteOpen`. **GAP FOTO BUKTI (dilaporkan, TIDAK dikerjakan)**: backend simpan `photo_url` string tapi TIDAK ada endpoint upload gambar & form foto — tak dipaksakan (scope terpisah). `go build` OK, `go vet` OK, `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`).

- [x] Fix audit final: karma/earnings dinamis di riwayat (A+B) + cleanup (C+D) — **PENDEKATAN (A+B)**: HITUNG ULANG dari berat tersimpan, TIDAK pakai kolom baru/migration. Backend satu-satunya sumber kebenaran: `models/pickup.go` tambah field transient `KarmaEarned int json:"karma_earned" gorm:"-"` & `EarningsEarned int64 json:"earnings_earned" gorm:"-"` (tidak di-persist), diisi on-the-fly oleh `fillEarnedValues()` (service `pickup_service.go`) utk setiap pickup COMPLETED yang dikembalikan `GetUserHistory` & `GetCollectorHistory` — memakai `calculateKarma` (60/24/6 per kg) & `calculateEarnings` (3000/1200/300 per kg) yang sama persis dgn ConfirmPickup, jadi angka riwayat selalu selaras. **FIX A**: `OrderHistory.tsx:87` hardcode `'+50 Karma'` → `+${order.karma_earned ?? 0} Karma`. **FIX B**: `collector/page.tsx` `totalKarma = completedMissions*25` & `'+25 Karma'` → total earnings Rupiah (`totalEarnings = Σ earnings_earned completed`) + label item `Rp {earnings_earned}` & kartu "Total Pendapatan" `Rp {totalEarnings}` (pilih earnings per-transaksi, lebih informatif daripada saldo `collector_earnings` utk statistik riwayat). **FIX C**: hapus `frontend/store/usePickupStore.ts` (dead store, konfirmasi tak diimpor di mana pun). **FIX D**: hapus komentar duplikat `CancelExpiredPendingPickups` di `repository/pickup_repo.go`. `go build` OK, `go vet` OK, `npm run build` sukses (TS clean), `npm run lint` 0 error (1 warning pre-existing `setAddresses`). Verifikasi API full flow 1/1/1 kg: warga `karma_earned=90`, kolektor `earnings_earned=4500`.

- [x] Comment sweep: English-only, prune noise, keep meaningful docs — `go build`/`go vet` OK, `npm run build` sukses (TS clean), `npx tsc --noEmit` OK. Komentar Indonesia → English, komentar restate/divider/redundan dihapus, komentar WHY/race/godoc/JSDoc dipertahankan. Catatan: `gofmt -l` masih menandai beberapa file — itu misalignment struct field & missing trailing newline yang PRE-EXISTING (bukan hasil sweep), `gofmt -w` sengaja TIDAK dijalankan karena akan mengubah alignment baris kode (melanggar constraint komentar-saja).

---
**Catatan penggunaan file ini:** update checkbox setiap kali sebuah task selesai *dan* terverifikasi manual (lihat RULES.md). Jangan centang berdasarkan "kode sudah ditulis" saja.